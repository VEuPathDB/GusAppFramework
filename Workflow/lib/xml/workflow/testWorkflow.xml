<workflow>

  <step name="firstStep"
        stepClass="GUS::Workflow::TestStep">
  </step>

  <step name="secondStep"
        stepClass="GUS::Workflow::TestStep">
  </step>

  <step name="thirdStep"
        stepClass="GUS::Workflow::TestStep">
    <depends name="firstStep"/>
    <depends name="secondStep"/>
  </step>

</workflow>
