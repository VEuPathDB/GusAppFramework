<workflow>

  <step name="firstStep"
        stepClass="GUS::Pipeline::Workflow::TestStep">
  </step>

  <step name="secondStep"
        stepClass="GUS::Pipeline::Workflow::TestStep">
  </step>

  <step name="thirdStep"
        stepClass="GUS::Pipeline::Workflow::TestStep">
    <depends name="firstStep"/>
    <depends name="secondStep"/>
  </step>

</workflow>
