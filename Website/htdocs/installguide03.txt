  # 
  # Gus setup
  #
  setenv JAVA_HOME /usr/local/j2sdk1.4.1_02/j2sdk1.4.1_02
  setenv ANT_HOME /opt/apache-ant-1.5.3-1
  setenv GUS_HOME ${HOME}/gus_home
  setenv PROJECT_HOME ${HOME}/projects
  setenv GUS_CONFIG_FILE ${HOME}/.gus.properties
  setenv PERL5LIB "${PERL5LIB}:${GUS_HOME}/lib/perl:${GUS_HOME}/lib/perl/CBIL"
  setenv PATH "${GUS_HOME}/bin:${PROJECT_HOME}/install/bin:${PATH}"
  setenv PATH "${ANT_HOME}/bin:${PATH}"

