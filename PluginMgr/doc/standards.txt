- use die to fail

- always test mkdir and open and die if they fail.

- never fail.  always use status, and exit gracefully.

- never use print, use log:
   - logData
   - logFatal
   - logAlert
   - logVerbose 
   - logVeryVerbose

- args are of the form firstSecond not first_second

- loading plugins need to be called LoadXXXX

--verbose --veryVerbose
--objVerbose --objVeryVerbose
